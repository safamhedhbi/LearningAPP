<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.safam.myapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_quiz_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_quiz_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Choix_CQE" />
        <activity android:name=".Matieres" />
        <activity android:name=".Cours" />
        <activity android:name=".Quiz" />
        <activity android:name=".Examen" />
        <activity android:name=".Login" />
        <activity android:name=".preExamen" />
        <activity android:name=".About"></activity>
    </application>

</manifest>